type {{{name}}} struct{
	{{#each properties}}
	{{#if prop}}
	{{{convertFirstCharToUpperCase (underlineToHump name)}}} {{base_java}} `json:"{{prop}},omitempty"`;
	{{else}}
	{{{convertFirstCharToUpperCase (underlineToHump name)}}} {{>type parent=../name}} `json:"{{name}},omitempty"`;
	{{/if}}
	{{/each}}
}

{{#if enums}}
	{{#each enums}}
	{{#if description}}
	/**
	* {{{escapeComment description}}}
	*/
	{{/if}}
    type  {{convertFirstCharToUpperCase name}} string

	const (
		{{#each enum}}
		{{{convertFirstCharToUpperCase (camelCase name)}}} {{convertFirstCharToUpperCase name}} = {{convertSingleQuotesToDoubleQuotes value}}
		{{/each}}
	)
	{{/each}}
	{{/if}}
