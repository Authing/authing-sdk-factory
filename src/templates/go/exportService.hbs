package client

{{!-- {{#if imports}}

{{/if}} --}}
import "authing-go-sdk/dto"

{{#each operations}}
    {{#equals method "POST"}}
	func (c *Client) {{{convertFirstCharToUpperCase name}}} (reqDto *dto.{{parametersBody.type}}) *dto.{{>result}}  {
        b, err := c.SendHttpRequest("{{{path}}}", constant.HttpMethodPost, "", nil)
        var response dto.{{parametersBody.type}}
		if err != nil {
		    return nil, err
	    }
        json.Unmarshal(b, &response)
	    return &response
	}
    {{/equals}}
    {{#equals method "GET"}}

    func (c *Client) {{{convertFirstCharToUpperCase name}}} (reqDto *dto.{{parametersDto}}) *dto.{{>result}}  {
        b, err := c.SendHttpRequest("{{{path}}}", constant.HttpMethodGet, "", nil)
        var response dto.{{parametersBody.type}}
		if err != nil {
		    return nil, err
	    }
        json.Unmarshal(b, &response)
	    return &response
    }
    {{/equals}}


	{{/each}}
