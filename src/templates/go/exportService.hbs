package client

{{!-- {{#if imports}}

{{/if}} --}}
import "authing-go-sdk/dto"

{{#each operations}}
	{{#equals method "POST"}}
{{!--     {{console this}} --}}
	func (c *Client) {{{convertFirstCharToUpperCase name}}} (reqDto *dto.{{parametersBody.type}}) *dto.{{>result}}  {
        b, err := c.SendHttpRequest("{{{path}}}", constant.HttpMethodPost, reqDto)
        var response dto.{{parametersBody.type}}
		if err != nil {
		    return nil, err
	    }
        json.Unmarshal(b, &response)
	    return &response
	}
    {{/equals}}
    {{#equals method "GET"}}
    func (c *Client) {{{convertFirstCharToUpperCase name}}} (reqDto *dto.{{parametersDto}}) *dto.{{>result}}  {
        b, err := c.SendHttpRequest("{{{path}}}", constant.HttpMethodGet, reqDto)
        var response dto.{{parametersDto}}
		if err != nil {
		    return nil, err
	    }
        json.Unmarshal(b, &response)
	    return &response
    }
    {{/equals}}
	{{/each}}
