public class {{{name}}} {
	{{#each properties}}
	{{#if description}}
	/**
	 * {{{escapeComment description}}}
	 */
	{{/if}}
	private {{>type parent=../name}} {{{name}}};
	{{/each}}

	{{#each properties}}
	public {{>type parent=../name}} {{{name_java_get}}}() {
        return {{name}};
    }
	public void {{{name_java_set}}}({{>type parent=../name}} {{name}}) {
        this.{{name}} = {{name}};
    }

	{{/each}}

	{{#if enums}}
	{{#each enums}}
	{{#if description}}
	/**
	* {{{escapeComment description}}}
	*/
	{{/if}}
    public static enum {{convertFirstCharToUpperCase name}} {
		{{#each enum}}
		{{{name}}}({{convertSingleQuotesToDoubleQuotes value}}),
		{{/each}}
		;

		private String value;

        {{convertFirstCharToUpperCase name}}(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }
    }

	{{/each}}
	{{/if}}

}
