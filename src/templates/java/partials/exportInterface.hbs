public class {{{name}}} {
	{{#each properties}}
	{{#if description}}
	/**
	 * {{{escapeComment description}}}
	 */
	{{/if}}
	private {{convertFirstCharToUpperCase name}} {{{name}}};
	{{/each}}

	{{#each properties}}
	public {{convertFirstCharToUpperCase name}} {{{name_java_get}}}() {
        return {{name}};
    }
	public void {{{name_java_set}}}({{convertFirstCharToUpperCase name}} {{name}}) {
        this.{{name}} = {{name}};
    }

	{{/each}}

	{{#if enums}}
	{{#each enums}}
	{{#if description}}
	/**
	* {{{escapeComment description}}}
	*/
	{{/if}}
    public static enum {{convertFirstCharToUpperCase name}} {
		{{#each enum}}
		{{{name}}}({{{value}}}),
		{{/each}}
		;

		private String value;
        {{name}}(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }
    }

	{{/each}}
	{{/if}}

};
